/* var prompt = require('prompt');

//
// Start the prompt
//
prompt.start();

//
// Get two properties from the user: username and email
//
prompt.get(['age'], function (err, result) {
  //
  // Log the results.
  //
  if (parseInt(result.age) === 1){
    console.log('  age: ', result.age);
  }
  console.log('Command-line input received:');
});
 */
console.log(12 % 6)
